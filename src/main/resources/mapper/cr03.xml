<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dksys.biz.user.cr.cr03.mapper.CR03Mapper">
    <select id="selectEstCount" resultType="int">
        SELECT COUNT(*)
        FROM TB_SM01M01 A
        WHERE 1 = 1
    </select>
   
    <select id="selectEstList" resultType="CamelMap">
    	SELECT * FROM ( SELECT T.* FROM (SELECT * FROM TB_SM01M01) T ) Z  WHERE file_trgt_key BETWEEN 0 AND 10
    </select>
</mapper>